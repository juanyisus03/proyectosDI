<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:pages="clr-namespace:EjercicioRecetas.Pages"
             x:Class="EjercicioRecetas.MainPage"
            Title="Recetas">

    <pages:MostrarRecetas/>
    <pages:CarouselRecetas/>
    <pages:BuscarRecetas/>

    <!--
    <ContentPage Title="RECETAS" IconImageSource="recetas.png" Background="LightYellow">

        <VerticalStackLayout HorizontalOptions="CenterAndExpand" Margin="10,10,10,10">
            <Label Text="Recetas Populares" FontAttributes="Bold" FontSize="Large" HorizontalTextAlignment="Center"/>

            <CollectionView x:Name="cvRecetas" ItemsSource="{Binding Recetas}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BackgroundColor="LightGray" Padding="20,7,20,7" Margin="0,10,0,10">
                            <StackLayout>
                                <Label Text="{Binding Nombre}" FontAttributes="Bold"/>
                                <Label Text="{Binding Descripcion}"/>
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>

    </ContentPage>
    <ContentPage Title="FAVORITOS" IconImageSource="favorito.png" Background="LightYellow">
        <VerticalStackLayout HorizontalOptions="CenterAndExpand" Margin="10,10,10,10">
            <Label Text="Mis Recetas Favoritas" FontAttributes="Bold" FontSize="Large" HorizontalTextAlignment="Center" Margin="0,0,0,20"/>
            <Label Text="Aquí aparecerán tus recetas favoritas" FontSize="Header" HorizontalTextAlignment="Center" TextColor="LightGray" />

            <CarouselView x:Name="crlRecetas" ItemsSource="{Binding Recetas}" HeightRequest="350" IndicatorView="indicator">
                <CarouselView.ItemTemplate>
                    <DataTemplate>

                        <Frame BackgroundColor="White" BorderColor="LightGray" HeightRequest="250">
                            <StackLayout>
                                <Label Text="{Binding Nombre}" FontAttributes="Bold"/>
                                <Image Source="{Binding RutaImg}" Aspect="AspectFill" WidthRequest="100" HeightRequest="100" HorizontalOptions="Center"/>
                                <Label Text="{Binding Descripcion}"/>
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CarouselView.ItemTemplate>
            </CarouselView>
            <IndicatorView x:Name="indicator" HorizontalOptions="Center" IndicatorSize="20"/>

        </VerticalStackLayout>
    </ContentPage>
    <ContentPage Title="BUSCAR" IconImageSource="buscar.png"  Background="LightYellow">
        <VerticalStackLayout HorizontalOptions="Center" Margin="10">
            <Label Text="Buscar Recetas" FontAttributes="Bold" FontSize="Large" HorizontalTextAlignment="Center" Margin="0,0,0,20"/>
            <SearchBar x:Name="buscador" Margin="0,0,10,0" TextChanged="SearchBar_TextChanged"/>
            <CollectionView x:Name="cvBuscados" ItemsSource="{Binding RecetasBuscadas}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BackgroundColor="LightGray" Padding="20,7,20,7" Margin="0,10,0,10">
                            <StackLayout>
                                <Label Text="{Binding Nombre}" FontAttributes="Bold"/>
                                <Label Text="{Binding Descripcion}"/>
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ContentPage>
        -->
</TabbedPage>
